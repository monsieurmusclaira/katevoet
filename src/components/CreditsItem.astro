---
const { crewfunction, crewname } = Astro.props;
---
{crewfunction && <p class="font-light md:text-right">{crewfunction}</p>}
<p class={`font-bold ${crewfunction ? '' : 'md:col-start-2'}`}>{crewname}</p>